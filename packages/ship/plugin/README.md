# @jobgetapp/ship-plugin

## Project description

The internal plugin engine for the `@jobgetapp/ship-cli` command line utility. Provides wrappers around command and docker execution. This library is not intended to be used directly.